apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: loader-db
  namespace: alerthub
spec:
  serviceName: loader-db
  replicas: 1
  selector:
    matchLabels:
      app: loader-db
  template:
    metadata:
      labels:
        app: loader-db
    spec:
      containers:
      - name: loader-db
        image: mysql:8.0
        ports:
        - containerPort: 3306
        env:
        - name: MYSQL_ROOT_PASSWORD
          valueFrom:
            secretKeyRef:
              name: alerthub-secrets
              key: MYSQL_ROOT_PASSWORD
        - name: MYSQL_DATABASE
          value: LoaderDB
        volumeMounts:
        - name: loader-db-storage
          mountPath: /var/lib/mysql
  volumeClaimTemplates:
  - metadata:
      name: loader-db-storage
    spec:
      accessModes:
      - ReadWriteOnce
      resources:
        requests:
          storage: 1Gi
