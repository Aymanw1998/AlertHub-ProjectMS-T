export function PlaceholderPage({ title }: { title: string }) {
  return (
    <div className="card">
      <h2>{title}</h2>
      <p>This module shell is ready. Endpoint integration is next.</p>
    </div>
  )
}
